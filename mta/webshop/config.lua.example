-- webshop/config.lua.example
-- Copy to config.lua and adjust as needed
return {
  -- Polling behavior
  pollIntervalMs = 5000,
  batchSize = 10,
  characterIdElementData = "dbid",
  -- When true, if target player is offline, insert item directly into items table
  fallbackToDBInsert = true,
}
